[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-ra --strict-markers --strict-config"

asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"

markers = [
    "db: test requires a live PostgreSQL test database (deselect with -m 'not db')",
]