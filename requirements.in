# Direct dependencies only. requirements.txt is the frozen lockfile.
# Regenerate: uv pip compile requirements.in -o requirements.txt

fastapi
uvicorn
asyncpg
pydantic
pydantic-settings
beautifulsoup4
lxml
requests
sentence-transformers
pgvector
anthropic