# Direct dependencies only. requirements.txt is the frozen lockfile.
# Regenerate:  pip freeze > requirements.txt
# NOTE: manually add these two lines to the TOP of requirements.txt after regenerating
# (pip freeze does not write them):
#   --index-url https://pypi.org/simple
#   --extra-index-url https://download.pytorch.org/whl/cpu
fastapi
uvicorn
asyncpg
pydantic
pydantic-settings
beautifulsoup4
lxml
requests
sentence-transformers